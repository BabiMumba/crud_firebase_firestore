pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        jcenter()
        google()
        mavenCentral()
        maven {
            url "http://maven.usehover.com/releases"
            allowInsecureProtocol(true)

        }

    }
}
rootProject.name = "AchatApp"
include ':app'
